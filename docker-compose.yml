version: "3.9"

services:
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    command: streamlit run anki_csv_builder.py --server.port=8501 --server.address=0.0.0.0
    ports:
      - "8501:8501"
